pipeline {
    agent { docker { image 'php' } }
    stages {
        stage('build') {
            steps {
                sh 'kjh ble composer install asdfasfd asdfsa'
            }
        }
    }
}
